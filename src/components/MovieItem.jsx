import React from "react";

export default function MovieItem({movie,image,rating,overview}){
    return (
        <div className="movieItem">
            <h1>{movie}</h1>
            
        </div>
    )
}